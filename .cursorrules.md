# Next.js Build & Suspense Rules
- **Suspense Boundary Requirement**: 所有位于 `app/(marketing)/` 目录下的 `page.tsx` 必须使用 `Suspense` 包裹主要内容。这是为了防止 `useSearchParams()` 导致的构建错误。
- **Dynamic Rendering**: 所有的营销页面（Marketing Pages）必须包含 `export const dynamic = "force-dynamic";`。
- **Client Component Logic**: 如果页面需要使用 `onClick` 或其他交互，必须在文件顶部保留 `"use client";`，并确保交互逻辑不直接暴露在服务端渲染的上下文中。
- **Code Integrity**: 在修复构建错误时，严禁删除原有的业务逻辑代码（如复杂的 section 或 footer），除非用户明确要求删除。
- **Search Params Bailout**: 记住 `useSearchParams()` 会导致 Next.js 在静态生成时跳过整个页面，因此必须配合 `Suspense` 使用。