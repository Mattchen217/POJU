"use client";

import { Suspense } from "react";

/**
 * Classic / Workspace floating switcher — retired.
 * Workspace is the default shell; keep this stub so old imports stay harmless.
 */
export function UiShellSwitcher() {
  return <Suspense fallback={null}>{null}</Suspense>;
}
