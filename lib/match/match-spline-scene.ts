/** Match Spline scene — homepage card, hero, analyzing wait. */
export const MATCH_SPLINE_SCENE = "/spline/Match.splinecode";

export const MATCH_SPLINE_CARD_ZOOM = 0.075;
export const MATCH_SPLINE_HERO_ZOOM = 0.11;
/** PWA hero — narrower viewport; pull camera back further so both circles fit. */
export const MATCH_SPLINE_HERO_PWA_ZOOM = 0.085;
/** CSS scale on hero scene root — shrinks on-page footprint without cropping canvas. */
export const MATCH_SPLINE_HERO_DISPLAY_SCALE = 0.62;
export const MATCH_SPLINE_HERO_PWA_DISPLAY_SCALE = 0.45;
/** CSS scale on card scene root — card viewport is small; zoom + scale both pull back to show both circles. */
export const MATCH_SPLINE_CARD_DISPLAY_SCALE = 0.55;
export const MATCH_SPLINE_ANALYZING_ZOOM = 0.52;
