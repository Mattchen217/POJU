/** Match orbs Spline — analyzing wait load/unload loop. */
export const MATCH_ORBS_LOOP_VISIBLE_MS = 4000;
/** Brief gap while WebGL unmounts before the next load. */
export const MATCH_ORBS_LOOP_HIDDEN_MS = 280;
