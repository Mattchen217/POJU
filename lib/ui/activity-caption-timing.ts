/** Rotate interval for status captions in Spline / loader activity animations (site-wide). */
export const ACTIVITY_CAPTION_ROTATE_MS = 5200;

/** Marketing carousel / how-it-works step copy — slightly slower than chat loaders. */
export const ACTIVITY_CAPTION_RING_ROTATE_MS = 7800;

/** CSS animation duration when activity caption text changes. */
export const ACTIVITY_CAPTION_FADE_DURATION = "0.9s";
