import type { ReactNode } from "react";

/** Dev audit tools: no marketing chrome beyond locale shell. */
export default function AuditRouteLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
