POJU 交付报告 P1–P6 产品规格文档（v2 · 代码校准版）
上一版是我凭空写的通用模板，跟你们真实系统对不上，作废。 本版依据两个信息源交叉核对写成：

你上传的 pivot-八页交付验收标准.md（Canonical 验收标准，内容标注最后更新 2026-09-01）
现拉取的 GitHub 远程库 github.com/Mattchen217/POJU（HEAD 0b9802d，lib/poju/report-blueprint.ts 文件修改时间为今天，是最新状态）
下面每一条规格后面都标了代码锚点（文件路径 + 行号），可以直接跳过去核对。

0. 先纠正一个命名混淆 + 一处文档与代码的真实漂移
命名：文件叫"八页"，但内容和代码是一致的 6 页正文（P1–P6）+ 附录。代码里原话（lib/poju/report-blueprint.ts:3）：

"定义第4段交付的 6 页正文 + 附录（原八页；已退役独立 thirty_day）"

thirty_day（原四周甘特页）仍作为 legacy key 留在 delivery-schema.ts:13-22 里，只为旧会话渲染，不在当前 DELIVERY_SEGMENT_KEYS 的 active shelf 里，新交付禁止再产出。

漂移点（你上传的标准文档已经不是最新了，这是实证）： 该文档第 309–320 行写的链路是 "bootstrap: P1 单独 → Wave A: P2∥P4 随时；P3 等 P1"。但我核对了当前真实 DAG（lib/llm/pro/delivery/dispatch/task-dag.ts:82-95）：foundation（P2）、science_action（P3）、metaphysics_action（P4）三个 assign 任务在 DAG 初始化时就是 locked: false，互相之间没有依赖，从任务创建那一刻就并行可跑，谁也不等谁。真正有依赖关系的是 Wave B 闸门（task-dag.ts:104-112）：risk_guard（P5）、signals_close（P6）被锁死，必须等 direct_answer（P1）+ science_action（P3）+ metaphysics_action（P4）三个都 ready 才解锁——注意 foundation（P2）不在这个闸门依赖里，P5/P6 理论上可以在 P2 还没写完时就开始生成。

这条我会在下面第三节标出来，其余部分我按代码现状写，不再按旧文档的描述转述。

一、六页总览表（对齐 report-blueprint.ts SSOT）
页	id（段 key）	标题 zh/en	path_role	input_role	是否挂依据层	所属 Wave
P1	direct_answer	核心直答 / Core Answer	verdict（定调）	chart_only	不挂（transition page）	Bootstrap（最早就绪）
P2	foundation	归因剖析 / Root Analysis	evidence（依据）	chart_only	挂	Wave A
P3	science_action	破局策略 / Breakthrough Strategy	primary_and_backup（主辅展开）	needs_reality	挂	Wave A
P4	metaphysics_action	东方谋略 / Eastern Stratagem	supports_primary	needs_reality	挂	Wave A
P5	risk_guard	风险预警 / Risk Guard	supports_primary	needs_reality	挂	Wave B（锁，等 P1+P3+P4）
P6	signals_close	行动建议 / Action Suggestions	closing	needs_reality	挂	Wave B（同上）
附录	appendix	结构数据与术语说明	appendix	chart_only	不适用	装配时合并，非独立生成任务
代码锚点：lib/poju/report-blueprint.ts:46-185（各页 purpose/writes/chart_inputs/path_role 原文）；lib/llm/pro/delivery/delivery-schema.ts:13-30（DELIVERY_SEGMENT_KEYS 与 DELIVERY_TRANSITION_KEYS）。

二、逐页规格
每页四段：定位（产品为什么要这页，抄自 report-blueprint.ts 的 purpose 字段并做口语化）、验收标准（来自你上传标准第 3/5/5A 节，按页拆解）、用户价值、实现方案（真实代码路径：blueprint → page-plan 派工 → page-prompt 人设 → gate）。

P1 · direct_answer（核心直答）
定位 用户耐心答完多轮收集，一打开报告最想要的是直接的答案，不是铺垫论证。这一页只给"结论的头"：正面直答 + 一句主路径 + 一句为什么。（原文见 report-blueprint.ts:51-53）

验收标准（对照上传标准 §5.1、表格 P1 行）

☐ 正面直答 + 主辅对照；core_conclusion 必须用短列表点名本页要兑现的 2–3 条可删依据主张，禁止只写口号结局（page-prompts/p1-direct-answer.ts FINALIZE_DUTY）
☐ 动态主副标题贴本案，不是空泛的"深度分析"
☐ 本页不挂依据层——UI 上不展开命理依据（delivery-schema.ts:27 DELIVERY_TRANSITION_KEYS 显式把 direct_answer 标为 transition：跳过 evidence + mark 两个 LLM 调用）
☐ 不报具体日期点位，只报阶段趋势 + 条件成熟
☐ forbid 清单命中即打回：multi_dim_all、metaphysics_pack_full、"东方/科学药方展开"（page-plan/build-page-plan.ts:44-51）
用户价值 用户不需要读完六页才知道系统建议怎么走；第一页就拿到明确判断和方向，降低阅读门槛和焦虑感。

实现方案

input_role: chart_only（report-blueprint.ts:55）——纯命理判断能写准，不需要议程收集现实信息
must_use 料：situation_conclusion、key_crossroads、primary_path、action_plan、question_expectation（build-page-plan.ts:42-43）
管线最短：这是唯一跳过 evidence + mark 阶段的页面，task-dag.ts:83-93 显示 P1 只有 fill → ready 两步，依赖仅自身 finalize spine，是六页里最先能标记 ready 的一页，Wave B 闸门也把它当第一必要条件
P2 · foundation（归因剖析）
定位 论证 P1 那个结论——用命理讲透"你为什么会卡在这困境里"，让用户相信后面给的药方不是随口说的。P1 给结论，P2 给论证，两页不重复（report-blueprint.ts:64-65）。

验收标准（对照上传标准 §3 表格 P2 行 + §5.2 护城河条）

☐ 多表象对症：收集阶段的多个真实表象，每卡"表象+本质"，末卡收敛到"因此主辅成立"
☐ 删依据自检：去掉 bazi_basis 后关键判断必须垮掉，不能"谁都适用"（全站铁律，上传标准 §2.1）
☐ 禁止执行步骤/月路线图混进本页——近阶清单归 P6，P2 至多一句阶段定位（上传标准 §5.2 明确勾选项）
☐ core_conclusion 用短列表写出 2–4 条本盘将要证明的结构阻力，禁止把用户原话直接当成已成立的表象（page-prompts/p2-foundation.ts FINALIZE_DUTY 原文）
用户价值 "顿悟时刻"——用户第一次看到一个自己没意识到的结构性归因，而不是被复述一遍自己已经知道的问题。这是六页里最容易被用户截图分享的一页。

实现方案

must_use：energy_structure、multi_dim_all、metaphysics_pack_dashboard、situation_conclusion、key_crossroads、question_expectation；forbid：metaphysics_pack_full、"执行步骤"、"复读P1结论头"、"编造生活剧情"（build-page-plan.ts:52-63）
chart_inputs 五项：energy_structure、element_scores、four_pillars_ten_gods、shen_sha_life_stage、current_da_yun_cycle（report-blueprint.ts:67-72），"按论证需要放，不为凑齐而凑"
唯一不在 Wave B 闸门依赖里的 Wave A 页面——理论上 P5/P6 可以在 P2 未完成时先跑，是本节开头指出的文档-代码漂移点
P3 · science_action（破局策略 · 科学一套）
定位 用户花钱的核心之一：科学这一套"怎么办"。从命理推出只对他成立的【策略+手段】，不是只有策略的半套（report-blueprint.ts:81-84）。

验收标准（对照上传标准 §2.3 表格、§5.2 护城河条）

☐ 策略+手段成套：每条论点必须同时有决策策略和可对照的科学手段（资源精力配比/沟通协作原则/节奏杠杆/一层示意），持久化格式是标记好的 **策略:** / **手段:** 双段（rx-argument-shape.ts:5-8, 18-23），前端按双段布局解析渲染
☐ 主辅各 angles ≥ 3，每轨互补维 ≥3（上传标准 §5.2 P3 条）
☐ 禁合同条款/完整话术剧本/专业代做；禁整页只推销主路径（build-page-plan.ts:70-77 forbid：东方色向清单、复读P1口号、合同话术长剧本）
☐ 删依据自检：去掉 bazi_basis 后谁都适用 → 废稿（page-prompts/p3-science-action.ts "删 bazi_basis 后谁都适用→废稿"原文）
用户价值 可执行、可信、独属于他的行动策略——不是"多沟通、保持耐心"这类任何人都能给的泛化建议。

实现方案

input_role: needs_reality：命理只给方向，要靠议程收集的现实信息（手上资源/可投入时间精力/执行力短板/对主路径的接受度，二元案额外收对方角色权力位等）才写得可执行（report-blueprint.ts:92-102）
质量策略是"来自候选菜单，不靠 sanitize 打回碰运气"——即手段内容源头是 user 侧生成的【P3 科学手段候选菜单】，而不是生成后再用规则打回重试赌质量（page-prompts/p3-science-action.ts 头部注释原文）
Wave A 三张深挖页之一，走完整 assign→write→mark→ready 管线（task-dag.ts:96-99 WAVE_A_DEEP），且是 Wave B 闸门的三个必要条件之一
P4 · metaphysics_action（东方谋略 · 双核暗锦囊）
定位 **东方谋略页**：相对 P3 明战术的暗锦囊。锚定【用户问题+期望】，底层 **八字（知己）+ 奇门（知局）** 双核本地真算，正文落地三柱——**局势交锋 · 意象调频 · 行为仪轨**；不挂主辅双轨（主辅已由 P3 锚定）。权威规格锁：`.cursor/docs/P4-东方谋略-规格锁.md`；blueprint 见 report-blueprint.ts metaphysics_action。

验收标准（对照上传标准 §2.3、§5.2 + 规格锁）

☐ 按域（东方谋略 vs 科学执行）切分，不按物（策略 vs 手段）切分——每维仍是策略+手段成套
☐ 双核：Fact-pack 须有八字喜用相关锚 + 奇门锁盘块（`qimen_cast_at` 幂等）；奇门失败重试后仍失败 → 本页 fail，**禁止**降级半套交付
☐ 正文三柱有料才写；仪轨仅白名单（时间/生理/空间锚），禁神棍道具/符咒/宗教摆设
☐ 反物化：禁止"缺水=水边/绿植、缺火=晒太阳"这类五行物化清单
☐ 禁复读 P3 工具手段（合同/股权/律师/Excel/文档模板等词族）；禁再写主辅双轨（build-page-plan.ts forbid 同步）
☐ 软译走既有 SSOT（含奇门 `qm_*`），禁止 moat-feed 另起第二套对照表；用户正文零裸专名
☐ 五行语义同源：生成与校验同读 lib/glossary/wuxing-semantic-ssot.ts；moat pass/fail 页级判定
☐ 过渡期旧维名 timing/polarity/archetype 可映射三柱，新菜单/duty 以三柱叙事为准（Step2）
用户价值 护城河真正落地——「八字知己、奇门知局」的东方时空博弈智囊；换盘换局策略应变，不是通用心理课，也不是第二份 P3。

实现方案

must_use：question_expectation、multi_dim_filtered（p4Indices）、energy_retune_frame、metaphysics_pack_full；**Step1 起**另接交付 Fact-pack 奇门摘要（复用 lib/qimen，与 Syncro 同源）
质量策略："来自候选菜单，不靠 p4_* 硬闸打回碰运气"；菜单生长三柱候选（Step2）
input_role: needs_reality，现实需求很轻：仅"现居/工作空间大致朝向或可调范围（可选）"；本页主要靠双核真算撑
锁盘：首次成功算盘写入 Fact-pack，之后只读；Lab 换盘须显式清奇门块
P5 · risk_guard（风险预警 · 执行刹车）
定位 执行 P3/P4 行动时的结构刹车——用户照药方动手时，提醒他这类结构特有的坑，不是另开一门人生课，也不是只复读 P1 的泛泛注意事项（report-blueprint.ts:130-134）。

验收标准（对照上传标准 §3 表格 P5 行、§5A "P5 逐条依据"硬闸）

☐ 每条熔断 = 出现→该做→注意→禁做（红灯/特有坑/切辅/防护），且必须能指回具体是执行 P3 还是 P4 的哪条动作时会栽
☐ 坑必须从 ji_shen（忌神）/ blind_spots（盲区）/ 负向多维 / path_costs 长出，不是凭空编
☐ 禁编造 Brief/议程未确认的时限 KPI
☐ 工程硬闸：每条 RiskItem = 一个 argument/依据槽，禁止把四个桶合并成一条共用依据（上传标准 §5A"P5 逐条依据"，代码锚点 page-schema/render.ts）
用户价值 防止用户"拿着药方一头栽进自己最容易犯的老毛病里"——这是把诊断和药方真正落到"能安全执行"的最后一道保险。

实现方案

must_use：multi_dim_risk（经 filterMultiDimIndicesForRisk() 过滤出的负向相关子集）、metaphysics_pack_polarity、self_check_negative（splitSelfCheckSignals() 拆出的负向部分）、primary_path、backup_path、question_expectation、action_brief（build-page-plan.ts:35, 89-98）
锁在 Wave B：task-dag.ts:104-112 显示 risk_guard 的 assign 任务 deps 包含 WAVE_B_GATE_ID，必须等 P1+P3+P4 全部 ready 才解锁，因为它的坑要能"指回将执行的 P3/P4 动作"，缺了上游就没有可指回的对象
action_brief 这个输入不是把 P2–P4 全文塞进 P5 的 prompt，而是先过 page-schema/action-extractor.ts 纯代码抽取（不经 LLM）成结构化 P5ActionBrief，只带 means/hard_metrics/source_anchors 等字段——这是"薄喂"原则的具体实现，避免上下文爆炸也避免 P5 自己再幻觉出新的行动项
P6 · signals_close（行动建议 · 出门仪式）
定位 一次性产品的收尾——读完要有"我拿到完整打法，可以出发了"的底气。吸收了已退役的 thirty_day 页的价值，但不是四周甘特表（report-blueprint.ts:157-160）。

验收标准（对照上传标准 §3 表格 P6 行、§5.1 "④自检"条）

☐ 身份对照 + 为何切换 + 金句用法 + 今晚一件事的闭环 + 近7日可勾选微清单 ≥4 条 + 带走三样
☐ 正向信号自查（self_check_signals 的正向拆分部分）
☐ 一次性独立收尾，无回来追踪钩子；禁止四周甘特；禁止第三次药方总结（page-prompts/p6-signals-close.ts FINALIZE_DUTY："禁止回来追踪/订阅钩子；禁止四周甘特；禁止第三次药方总结"原文）
☐ 今晚/近7日候选必须指回【P6 出门候选菜单】的具体条目或绑定 means_candidate_ref，措辞可改写但禁止与 P3 逐字复读（同上文件 buildFillDuty 部分）
用户价值 消解"读完一堆分析然后呢"的落空感，给用户一个今晚就能做、近一周可对照执行的具体闭环，是复购/口碑传播的关键触点。

实现方案

同样锁在 Wave B，依赖与 P5 相同的 WAVE_B_GATE_ID（task-dag.ts:104-112）
must_use：self_check_positive、rhythm_frame、action_plan、primary_path、backup_path、action_brief；forbid：第三套药方、四周表、逐字复读P3（build-page-plan.ts:105-116）
人设明确写成"交付书定稿师 · 出门仪式收尾（非第三次药方写手）"（page-prompts/p6-signals-close.ts FINALIZE_DUTY），和 P5 的人设"执行护栏写手（非励志教练、非出门仪式司仪）"刻意互相排斥，防止两页在改提示词时职责漂移到彼此身上
附录 · appendix
定位：透明存档，可复盘，不承担决策叙事（report-blueprint.ts:179）。实现方案：chart_inputs 只有 structured_raw，input_role: chart_only，且不是独立生成任务——在 FINALIZE_ASSEMBLE_ID / 最终 assemble 阶段与六页正文一起装配（task-dag.ts:70-76, 114-122），不占用六页各自的 assign/write/mark 管线。

三、跨页工程规范（校准后）
3.1 真实生成 DAG（替换上传标准第 10 节的过时描述）
job 启动
  → 六个 finalize spine（P1–P6）同时创建，互相无依赖，并行起跑
  → P1（direct_answer）：finalize → fill → ready（跳过 evidence/mark，最先就绪）
  → P2/P3/P4（foundation / science_action / metaphysics_action）：
      assign 从一开始就是 unlocked，三者互相不等，各自走 assign→write→mark→ready
  → WAVE_B_GATE：等 P1 + P3 + P4 三者 ready（不含 P2）
  → P5/P6（risk_guard / signals_close）解锁后走完整管线
  → assemble：等六页全部 ready + 附录装配，成书
代码锚点：lib/llm/pro/delivery/dispatch/task-dag.ts:63-122

3.2 重试与容错（真实数值，替换我上一版的臆测数字）
项	真实值	说明	锚点
单次生成单元重试上限	1 primary + 1 corrective = 2 次，硬止	产品规则明确写"从不 3+"，宁可先把 feed/prompt 做对，也不靠重试撞质量	delivery-retry-policy.ts:16-17
应用层重试开关	DELIVERY_ENABLE_RETRIES = false（主开关关闭）		delivery-retry-policy.ts:20
单段 soft-wall /continue 跳数上限	8	纯为应对 Vercel 300s 硬杀的时钟续跑，不是质量重试	delivery-retry-policy.ts:26-29
任务级绝对墙钟	40 分钟（从 created_at 起算）	即便心跳/soft-wall 一直 ok:true 也会触发，独立于任何阶段计数器	delivery-retry-policy.ts:35-38
任务级 continue 跳数硬顶	18	防止无声 soft-wall 空转	delivery-retry-policy.ts:40-43
Vercel 单次 invoke 硬杀	300,000ms（export const maxDuration = 300）	到点直接被平台杀掉，不是软性超时	final-delivery-stage-runner.ts:126-127
3.3 质量哲学：候选菜单优先于生成后打回
P3/P4/P5/P6 四页的提示词文件头部注释都写了同一句话的变体："质量优先：means/RiskItem 来自 user 侧【候选菜单】，不靠 sanitize/硬闸打回碰运气"。也就是说校准的重点不是在输出端堆更多正则/分类器去拦截坏结果（这是我上一版臆测方案的思路），而是在输入端先用代码生成一份结构化候选池，LLM 从池子里选和改写，从源头压缩"编造"的空间。这一点是你们跟我上一版方案最大的方向性差异，值得写进任何后续改动的第一原则里。

3.4 段落形状硬闸（防空心页）
narrative-shape-gate.ts:9-10 明确说这个闸是为了防止"重复的 body 键导致 JSON.parse 后只剩一条，模型返回单个大блоб"——检测逻辑是扫描 arguments 数组里每个对象内 "body" 键出现次数，≥2 次判定为形状异常，触发段级重试（仍受 3.2 的 2 次硬顶约束，不会无限重试）。

四、上传标准中"每次改交付后必跑"的清单（原样保留，未改动）
这部分你上传的文档写得很完整，跟代码核对后没有发现过时，我直接保留，不重复造轮子：

上传标准 §5.1「用户价值」四项必过勾选
上传标准 §5.2「护城河」P3/P4/P5/P6 各自硬判据 + "半套即失败"规则
上传标准 §5A 工程硬闸表（段数、段级重试、页级承重锚、Synthesis 主辅锚、Evidence 覆盖、P5 逐条依据、相邻金字、槽外禁短词、软译回声）
上传标准 §6「三盘对照」：职场/创业/感情三个差异大的命理结构各出一份报告，比对药方是否雷同、删依据是否垮掉
冒烟脚本原样有效：tsx scripts/test-delivery-narrative-arg-gate.ts、tsx scripts/test-delivery-mark-adjacent-gold.ts、tsx scripts/test-delivery-page-schema.ts。