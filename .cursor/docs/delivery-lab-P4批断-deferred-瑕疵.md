# Delivery Lab · P4 批断 / 正文 deferred

**状态**：派工 claim_seed 已改为纯结构；半截句只许用合格结构种子 soft-fill。**请重跑 P4 派工** →（claim 变了再批断）→ 再 fill  
**盘**：己土合伙续跑

## 铁律留痕

铁律 15：主修 prompt/菜单/派工契约；#6 漏放因 claim_seed 手段腔 + 悬挂「再」漏检，不是新加句式黑名单追稿。

## 已修（派工）

1. ref 跟 `moat_class`（`realignP4PreferBindingsToMoat`）  
2. 悬挂：此时 / 十神主 / **再·才·便·就**；soft-fill **仅** `isUsableAssignPreferClaim`  
3. `claim_seed` 去掉加码/兑现/借势/姿态等手段腔，只留用忌/运岁/十神结构  
4. fact-pack 软修后再验悬挂一次  

## Lab 顺序

1. 重跑 **P4 派工** — ref 同型；claim 完整结构句（可被种子 soft-fill）；无执行处方尾。  
2. 若 claim 变了 → 重跑批断。  
3. 再重跑 **P4 正文 fill**。
