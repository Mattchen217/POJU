# Delivery Lab · P4 批断 / 正文 deferred

**状态**：P3→P4 ActionBrief + DAG（P4 等 P1+P3 ready）已接；#7 过杀（coach/de-calc）已收口。**请准备重跑** P4 fill（须 P3 已 ready）  
**盘**：己土合伙续跑

## 产品定尺（冻结）

- P4 = 落实 **P1 主辅 + P3 具体 means** 时的东方自我调频；换盘换人即失效。
- 不写第二套主辅名；每维服务 `主路径推进` / `切辅条件` / `守成窗口` 之一。
- **leverage / avoid / field_matrix 保持退役**。薄页靠 dimensions。
- **上游**：fill 注入同一份 `extractP5ActionBrief`（无 P4 self）→ `formatP3MeansBriefForP4Retune`；assign/write **不**吃该 brief。DAG：P4 assign deps = P1 ready + P3 ready（复用 ready 粒度，先不做 fill-only 依赖）。

## assign / write 要不要动？

| 步 | 职责 | 本轮 |
|----|------|------|
| **assign** | 锁结构主张 + moat_class + 菜单 ref | 已钉「给 write/fill 留结构钩」；**不必为填正文重跑派工**，除非 claim 本身无机制钩 |
| **write** | 锁 professional_evidence（批断） | 已钉 evidence 须写满调频机制供 compress；**本盘若批断已有火旺/用忌/食神链，可只重跑 fill** |
| **fill** | 东方 means 正文 · **挂 P3 执行面** | 主修面；须能说清「做 P3 哪类动作时如何调频」 |

## Lab attempt #7 · `p4_means_thin`

**链路**：agency 软修丢句 → coach 误剥「技术输出+谈判筹码」→ de-calc 误杀观察者姿态维 → dens 空维。

**已收**：`谈判筹码` 为 soft coach（有技术输出/借势等东方标记则留）；de-calc 放行观察者/侧翼/借平台类；Lab/生产 fill 喂 P3 ActionBrief；P4 排队等 P3 ready。

## Lab attempt #5 · `p4_generic_means`（过杀）

有火旺/水旺/窗口/借势等标记的 means 不做 de-calc 删除；扩合伙宾语 frame。

## Lab attempt #4 · 闸过 · 产品 F

薄 means / 通用杠杆（定深前）。

**请准备重跑** P4 正文（确认 Lab 里 P3 science_action 已有 page_schema）。
