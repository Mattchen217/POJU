---
description: Eastern OS UI + 字体 + tokens — 金/黑/青；改 Workspace/交付外观时 @05-visual-language
alwaysApply: false
---

# Visual · Eastern OS（按需 @05-visual-language）

**SSOT**：本文件 + `styles/workspace.css` + `app/layout.tsx` / `styles/ds-tokens/fonts.css`。

## 色与外形

- 主色金 `#f2ca50` / `#d4af37`；次强调青 `#9cf0ff`
- 表面近黑 `#0b0f12` 等；毛玻璃半透明黑 + 细白边；**禁**紫主色 / 紫雾
- 圆角：控件 4–8px，卡片约 8–12px；主 CTA **矩形小圆角**，禁 pill（`9999px`）作默认主按钮
- 交付卡几乎满宽、窄 gutter（约 6–10px）
- 禁 workspace 交付流套旧紫系类（如 `poju-glass-card`）

## 字体（全站）

| 角色 | Token | 字体 |
|------|--------|------|
| UI | `--pj-font-sans` | Geist → Inter → Noto Sans SC |
| 诗意 | `--pj-font-verse` | EB Garamond |
| 等宽 | `--pj-font-mono` | JetBrains Mono |
| 中文仪式 | `--pj-font-serif-cjk` | Noto Serif SC |

禁旁支 webfont（Outfit / Plus Jakarta / Cinzel 等）。Canvas 用 `resolveSiteUiFontFamily()`。

## Token 纪律

视觉值走 CSS 变量 / 已映射 class；勿硬编码紫品牌色。间距优先 8pt 网格。仅暗色。动画克制；尊重 `prefers-reduced-motion`。

营销粒子页若需例外须单独说明；**默认仍跟本规则**。
