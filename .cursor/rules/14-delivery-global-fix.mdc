---
description: 交付铁律 — 修复必须是换人换盘仍成立的类别规则，禁止按当前 Lab 案例封禁原句
alwaysApply: true
---

# Delivery · 全局修复（铁律）

**每次改交付依据 / 正文 / 派工 / 批断 / 闸门 / 提示词 / assign / write / fill 之前必读。** 与 `11` `12` `13` 一起过。

## 铁律

修复必须是**类别**，换一个人、换一张盘、换一个问题，同一类错误仍被拦住，同一类合格批断仍留下。

禁止把当前 Lab 失败稿里的原句、干支、十神搭配、宫位名写成只针对这次的黑名单或正例。

## 合格的修法

- 类别：事实档没有写出的宫位；五行 / 十神生克表对不上的方向；没有本盘结构记号的短句。表是闭集（五行生克、十神生克），不是某一盘的例句。
- 闸门读的是**这次调用传入的事实档**。这张盘有的结构留下，没有的删掉或失败。
- 确定性删句、换摘录，不因此再调一次模型。删完没有结构关系，显式失败。
- 测试用另一张盘的事实档。当前失败稿只用来对照类别，不写进断言字符串当唯一靶子。

## 不合格

- 提示词或闸门里出现当前案例原句（例如只禁「配偶宫动荡」「内心纠结」「丙火冲克壬水」）。
- 用正例句教模型。写不全各种盘的合格样句，就不要样句。
- 把写反的生克自动改成反方向，当成新的批断。
- 为了让这一步变绿，放宽已经判对的类别（盘外宫位、摘录等于主张、生克写反）。

## 自检

1. [ ] 这条规则里有没有任何当前盘的原句、干支或十神搭配？有 → 改成类别，或回滚。
2. [ ] 换一张盘、换一个问题，这条还会拦同一类错、放同一类对吗？不会 → 回滚。
3. [ ] 测试是否用了另一张盘的事实档？
