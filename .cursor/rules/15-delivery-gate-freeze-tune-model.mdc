---
description: 交付铁律 — 闸门定死为验收尺后不再追改；主调模型提示词让首稿合格
alwaysApply: true
---

# Delivery · 闸门定尺，主调模型（铁律）

**每次改交付依据 / 正文 / 派工 / 批断 / 闸门 / 提示词 / assign / write / fill 之前必读。** 与 `11` `12` `13` `14` 一起过。

## 铁律（用户拍板 · 禁止再犯）

1. **闸门 = 评判标准**：先把「什么叫合格」写成稳定验收尺，定好后**不要**再用新失败句式去追着加正则、加删句、加特例。
2. **主修 = 模型输出**：首稿不合格 → 改 **prompt / 菜单 / 派工契约 / 喂给模型的材料**，让模型一次写出符合该尺的内容。
3. **闸门不是生成器**：确定性软修（删无结构句、按闭集表丢掉写反的生克）只做护栏与收口；**禁止**把「靠闸门越修越长」当成主路径。模型输出都不合格，闸门堆规则也变不成合格产品。
4. **尺本身要改时**：只允许因为验收定义错了（误杀合格、放行明确违规类别）而改闸门；改完立刻把同一要求写进提示词，并冻结。禁止「Lab 又绿又红 → 只加一条 strip」。

## 允许 vs 禁止

| ✅ 允许 | ❌ 禁止 |
|--------|--------|
| 冻结的深度 / 盘外 / 生克方向 / 本卡主张等**类别**验收 | 看见「受火熬煎」「亦暗示」就再加一条只挡这句的规则当主修 |
| 改 write / assign / fill 的 system·user，闭集表进提示词 | 闸门一轮、提示词零改，指望删句把自由发挥洗成批断 |
| 确定性软修与明示 fail（规则 11） | 质量失败再烧一轮 LLM 碰运气 |
| 发现尺有漏洞 → 改尺 + 同步改提示词 + 文档，然后停手 | 尺与提示词长期两套话 |

## 批断步（P2 write）现行尺（冻结 · SSOT）

代码：`lib/llm/pro/delivery/page-schema/deep-evidence-quality.ts`（`assessDeepEvidenceUnitDepth` + `stripSoftPaddingEvidence`）。

合格 evidence 必须同时满足：

1. 无 `⟦`；≥2 句；够长；每句是命理结构句（日主/柱干支/用喜忌/十神/合冲刑害/大运流年等）。
2. 生克方向落在闭集表上；写「克/生/受/被」须能落到表内双方，不能无主、不能反局。
3. 地支上的具体十神 = 该支**本气**对日主的十神；柱干十神不得贴到地支上。
4. 只展开本卡已锁 `unit_claim`；不得另起本卡主张以外的合冲刑害半合。
5. 禁止感受腔、职业/话语权类白话尾巴、一个十神配一句人生道理。

软修可删掉违反 2–5 的句子；删完若不满足 1 → **显式失败**，去改提示词，不在同 invoke 再调模型（规则 11/12）。

## 自检

1. [ ] 这次是改提示词/喂料让首稿合格，还是又在闸门上加句式？
2. [ ] 若改了闸门：验收定义是否真错了？提示词是否已同步同一条要求？
3. [ ] 换人换盘换问题，尺与提示词是否仍同一套（规则 14）？
