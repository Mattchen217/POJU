---
description: 四产品统一输出 + prefix 缓存省钱工程 — 保护性规范（必读，不可破坏）
alwaysApply: true
---

# 保护性规范（Canonical Specs）

**任何改动或新建**（提示词、LLM 调用链、渲染、合规、落库、会话结构）**必须先读并遵循**以下文档，不得破坏其中规定：

1. `.cursor/docs/pojulife-四产品统一输出规范.md` — 术语/排版/闭集/底座中立/合规/渲染链
2. `.cursor/docs/prefix-cache-省钱工程-spec.md` — provider.order 钉供应商、静态 system、base_analysis 一致、session_id 观测
3. `.cursor/docs/提示词按阶段隔离-防再污染备忘.md` — **交付向规则禁止进全阶段共用层**（打标/双层/金字/润色只走 phase 门控或该阶段 taskBlock）

## 提示词隔离 — 不可破坏要点（防再污染）

- **铁律**：交付专用规则绝不进恒在身份层 / 无门控控制面；要加 → phase 门控或该阶段 taskBlock
- **对话阶段**：不做 ⟦t:⟧ 硬塞教学；软译交后端 autoMark（含 delivered 短聊）
- **八页交付**：打标+双层自带（`final-delivery`），勿回灌进 chat 共用层
- **五段口径**：collecting 不定一主一辅；收敛在 synthesis；Call A 不喂 8 页蓝图
- 改提示词前过该备忘「自检清单」；详见 alwaysApply 规则 `08-prompt-phase-isolation.mdc`

## 四产品输出 — 不可破坏要点

- 用户可见术语：`⟦t:<闭集slug>|<软译>|<语境白话>⟧`；展示走 `GlossaryText` → `RichReadingText`，禁止裸 markdown
- 神煞闭集 9 个；集外禁词（元辰/六秀日/阴差阳错/空亡…）落库前 `auditDeliveredText` 必须能阻断
- 排版：`###` / `**要旨:**` / `> 金句` / 独占行 bullets / 短段；禁 `Bold lead:` 字面、禁字墙
- 底座中立四维（数字孪生）：不推职业/事件/医疗脏腑/逐柱罗列；下游靠 `structured` 作事实源
- 改 OpenRouter 路由：设 `OPENROUTER_PROVIDER_ORDER`（`provider.order` + `allow_fallbacks: false`）；`OPENROUTER_PROVIDER_IGNORE` 可合并。`session_id` 仅观测，不钉供应商

## Prefix 缓存 — 不可破坏要点（INV-1～6）

- **静态 system**（跨轮 byte 恒定）：`buildPojuCorePromptSections` + NA adaptation + profile/base_analysis
- **动态内容**（日期/语言/任务/议程/已收集上下文）只在 **user 消息**侧，不在 system
- `base_analysis` session 内快照复用，键序稳定；`session_id` 统一 `cache-session-id.ts` 生成器
- 勿频繁改静态头（一次性失效全部缓存）；勿在静态头混入时间戳/随机/非确定顺序
- TTL 分钟级 miss 是预期，非 bug；观测 `[openrouter] prefix cache` 日志的 `cached_tokens`

改动前过两份文档文末「自检清单」。
