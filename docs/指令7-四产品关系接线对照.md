# 指令 7 · Glyph / Syncro / Match 关系接线对照

> 共享 `relation-engine` + 闭集纪律（实例清单 / 守卫 / 落库门禁）。验收脚本：`pnpm exec tsx scripts/test-cross-product-relation-closed-set.ts`

---

## 共用模块

| 文件 | 作用 |
|---|---|
| `lib/llm/prompts/relation-closed-set-context.ts` | 单盘 / Match 闭集拼装 + audit allowlist |
| `lib/calculations/relation-engine.ts` | `computeCrossProfileBranchRelations`、`computeMatchRelationAuditAllowlist`、`buildMatchRelationInventoryBlock` |
| `lib/llm/services/delivery-audit-regen.ts` | `auditDeepStringFields(..., { relations })` + `relation_*` 临界失败 |

---

## Glyph

### 改动对照

| 位置 | 改动 |
|---|---|
| `glyph-deepseek-prompt.ts` | system 注入 `stitchSingleProfileRelationClosedSet`（本命 + 流年定向 + `POJU_OUTPUT_DATA_DISCIPLINE`） |
| `glyph-reading-service.ts` | 落库前 `buildSingleProfileRelationClosedSet` → `auditDeepStringFields(..., relations)`，临界失败 1 次低温重生成 |

### 数据面

- **本命关系**：`buildStructuredInstanceInventory`
- **流年当下质地**：`computeDirectedDynamicRelations` + `buildDirectedDynamicRelationInventoryBlock`（按用户问题粗分类过滤）

### 样例（合规输出片段 · 中文）

> 就你问的「感情里该坚持还是放手」，这支签照出的是**在暂停里看清方向**——结合你盘面里 ⟦t:chong_午_子|两股力的正面顶撞|子午之间的硬顶⟧，今年流年引动的是**外部节奏与内心步调不一致**，不必急着下结论，先把「你在怕失去什么」说清楚。

- ✓ 引用引擎算出的关系 id + 软译 + 中性
- ✓ 全文关系金字 ≤3
- ✗ 禁止裸写「子午相冲」「寅巳相刑」等未在闭集内的词

---

## Syncro

### 改动对照

| 位置 | 改动 |
|---|---|
| `syncro-deepseek-prompt.ts` | system 注入 `stitchSingleProfileRelationClosedSet`（`questionText` = `task_description`） |
| `syncro-reading-service.ts` | `fetchLlmAdviceBatch` 对 matrix + task_response 做 relation 审计 + 1 次 regen |
| `matrix-narrative-prompt.ts` | preview 矩阵叙事 `liunian_relations` 来自 `computeLiunianRelations`（时空=时+空，流年为核心） |

### 数据面

- **流年引动为主**：定向过滤随任务描述（事业/感情等关键词）
- 矩阵 rationale / task_response 可内化 1–2 条流年关系，不写清单

### 样例（合规 rationale 片段 · 中文）

> 这个时辰与你所在位置的真太阳时对齐，整体是 **following_current** 的节奏——你任务盘里今年 ⟦t:liunian_*|流年引动|…⟧ 指向**宜先理顺内部优先级再动身**，朝北偏东的窗口更适合谈条款而非硬推结论。

---

## Match

### 改动对照

| 位置 | 改动 |
|---|---|
| `match-deepseek-prompt.ts` | system 注入 `stitchMatchRelationClosedSet`（A/B 实例 + 守卫 + 双人动态 + discipline） |
| `match-analysis-service.ts` | `buildMatchRelationClosedSet` allowlist → 报告全文 audit + regen |
| `relation-engine.ts` | Match 原 branch-interactions 评分保留；**判定输出**改 `computeCrossProfileBranchRelations` 进闭集 |

### 数据面

- A/B 本命 + 各自流年/定向 + **今年双人动态**（cross_*）
- 关系描述默认 category = relationship

### 样例（合规段落 · 中文）

> 你们之间的协同底子仍在，但今年双人动态里 ⟦t:cross_*|…|A 日支与 B 月支的六合纽带⟧ 说明**合作面比对抗面更可用**——先把「谁在什么情境下容易退守」讲清楚，比追问谁对谁错更能推进关系。

---

## 验收清单

- [ ] `test-relation-engine.ts` — 引擎 28 项
- [ ] `test-relation-closed-set.ts` — S3 集成
- [ ] `test-cross-product-relation-closed-set.ts` — 三产品 prompt + audit
- [ ] 三产品输出可引用算出的关系；集外关系词被 `relation_*` 拦截
- [ ] 风格：金字、中性、克制（§9 + `POJU_OUTPUT_DATA_DISCIPLINE`）
